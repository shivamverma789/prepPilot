<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>PrepPilot — AI-Powered Placement Preparation for College Students</title>
  <meta name="description" content="PrepPilot helps college students crack placements with AI-powered roadmaps, ATS resume analysis, mock interviews, and DSA activity tracking. Free forever."/>
  <meta name="keywords" content="placement preparation, AI roadmap, resume builder, ATS resume analyzer, mock interview, DSA tracker, college students, campus placement"/>
  <meta name="robots" content="index, follow"/>
  <link rel="canonical" href="https://preppilot.in/"/>
  <meta property="og:title" content="PrepPilot — AI-Powered Placement Preparation"/>
  <meta property="og:description" content="Personalized roadmaps, ATS resume analysis, AI mock interviews, and DSA tracking — everything a student needs to crack placements."/>
  <meta property="og:url" content="https://preppilot.in/"/>
  <meta property="og:type" content="website"/>
  <meta property="og:image" content="https://preppilot.in/images/logo2.png"/>
  <meta name="twitter:card" content="summary_large_image"/>
  <meta name="twitter:title" content="PrepPilot — AI-Powered Placement Preparation"/>
  <meta name="twitter:description" content="Personalized roadmaps, ATS resume analysis, AI mock interviews, and DSA tracking — everything a student needs to crack placements."/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet"/>
  <style>
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    :root{
      --blue:#2563eb;--blue-dark:#1d4ed8;--blue-mid:#3b82f6;
      --blue-light:#eff6ff;--blue-pale:#dbeafe;
      --white:#fff;--gray-50:#f8fafc;--gray-100:#f1f5f9;
      --gray-200:#e2e8f0;--gray-400:#94a3b8;--gray-500:#64748b;
      --gray-600:#475569;--gray-700:#334155;--gray-800:#1e293b;--gray-900:#0f172a;
    }
    html{scroll-behavior:smooth}
    body{font-family:'Inter',sans-serif;background:var(--white);color:var(--gray-800);overflow-x:hidden}

    /* ── NAVBAR ── */
    .nav{
      position:fixed;top:0;left:0;right:0;z-index:100;
      height:64px;
      display:flex;align-items:center;padding:0 32px;gap:24px;
      background:rgba(255,255,255,0.85);
      backdrop-filter:blur(12px);
      border-bottom:1px solid rgba(226,232,240,0.8);
      transition:background 0.3s;
    }
    .nav-brand{display:flex;align-items:center;gap:10px;font-size:18px;font-weight:800;color:var(--gray-900);text-decoration:none;letter-spacing:-0.5px;flex-shrink:0}
    .nav-brand-icon{width:34px;height:34px;background:linear-gradient(135deg,var(--blue),var(--blue-mid));border-radius:9px;display:flex;align-items:center;justify-content:center}
    .nav-spacer{flex:1}
    .nav-links{display:flex;align-items:center;gap:8px}
    .nav-link{padding:8px 14px;border-radius:8px;font-size:14px;font-weight:500;color:var(--gray-600);text-decoration:none;transition:all 0.15s;white-space:nowrap}
    .nav-link:hover{background:var(--gray-100);color:var(--gray-900)}
    .nav-cta{padding:9px 20px;border-radius:9px;font-size:14px;font-weight:700;background:linear-gradient(135deg,var(--blue),var(--blue-mid));color:white;text-decoration:none;box-shadow:0 2px 8px rgba(37,99,235,0.3);transition:all 0.15s;white-space:nowrap}
    .nav-cta:hover{transform:translateY(-1px);box-shadow:0 4px 14px rgba(37,99,235,0.4)}
    .hamburger{display:none;background:none;border:none;cursor:pointer;color:var(--gray-600);padding:6px;border-radius:8px;flex-shrink:0}
    .hamburger:hover{background:var(--gray-100)}

    /* Mobile drawer */
    .mobile-menu{
      display:none;
      position:fixed;top:64px;left:0;right:0;
      background:var(--white);
      border-bottom:1px solid var(--gray-200);
      padding:16px 20px;
      flex-direction:column;gap:4px;
      z-index:99;
      box-shadow:0 8px 24px rgba(0,0,0,0.08);
      animation:menuSlide 0.2s ease both;
    }
    .mobile-menu.open{display:flex}
    @keyframes menuSlide{from{opacity:0;transform:translateY(-8px)}to{opacity:1;transform:translateY(0)}}
    .mobile-menu .nav-link{padding:12px 14px;font-size:15px;border-radius:10px;color:var(--gray-700)}
    .mobile-menu .nav-cta{text-align:center;padding:13px;margin-top:8px;border-radius:10px;font-size:15px}

    /* ── HERO ── */
    .hero{
      min-height:100vh;
      display:flex;flex-direction:column;align-items:center;justify-content:center;
      text-align:center;
      padding:120px 24px 80px;
      position:relative;
      overflow:hidden;
      background:linear-gradient(180deg, #f0f7ff 0%, var(--white) 60%);
    }

    /* Animated background grid */
    .hero-grid{
      position:absolute;inset:0;
      background-image:
        linear-gradient(rgba(37,99,235,0.06) 1px,transparent 1px),
        linear-gradient(90deg,rgba(37,99,235,0.06) 1px,transparent 1px);
      background-size:56px 56px;
      mask-image:radial-gradient(ellipse 80% 60% at 50% 0%,black 40%,transparent 100%);
    }

    /* Glowing orbs */
    .hero-orb{position:absolute;border-radius:50%;filter:blur(80px);pointer-events:none;animation:orbFloat ease-in-out infinite}
    .hero-orb-1{width:500px;height:500px;background:rgba(37,99,235,0.12);top:-100px;left:-100px;animation-duration:10s}
    .hero-orb-2{width:400px;height:400px;background:rgba(99,102,241,0.1);bottom:-80px;right:-80px;animation-duration:13s;animation-delay:-4s}
    .hero-orb-3{width:300px;height:300px;background:rgba(59,130,246,0.08);top:40%;left:60%;animation-duration:8s;animation-delay:-6s}
    @keyframes orbFloat{0%,100%{transform:translate(0,0) scale(1)}33%{transform:translate(20px,-20px) scale(1.05)}66%{transform:translate(-15px,15px) scale(0.97)}}

    .hero-content{position:relative;z-index:1;max-width:800px}

    .hero-badge{
      display:inline-flex;align-items:center;gap:8px;
      background:var(--blue-light);color:var(--blue-dark);
      padding:6px 16px;border-radius:20px;
      font-size:13px;font-weight:600;margin-bottom:28px;
      border:1px solid var(--blue-pale);
      animation:fadeUp 0.6s ease both;
    }
    .hero-badge-dot{width:6px;height:6px;border-radius:50%;background:var(--blue);animation:pulse 2s ease-in-out infinite}
    @keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:0.5;transform:scale(1.4)}}

    .hero-title{
      font-size:clamp(40px,6vw,72px);
      font-weight:900;
      line-height:1.08;
      letter-spacing:-2px;
      color:var(--gray-900);
      margin-bottom:24px;
      animation:fadeUp 0.6s 0.1s ease both;
    }
    .hero-title .accent{
      background:linear-gradient(135deg,var(--blue) 0%,#6366f1 100%);
      -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
    }

    .hero-sub{
      font-size:clamp(16px,2vw,20px);
      color:var(--gray-500);
      line-height:1.65;
      max-width:560px;
      margin:0 auto 40px;
      animation:fadeUp 0.6s 0.2s ease both;
    }

    .hero-actions{
      display:flex;align-items:center;justify-content:center;gap:14px;flex-wrap:wrap;
      animation:fadeUp 0.6s 0.3s ease both;
    }
    .btn-hero{
      padding:16px 36px;border-radius:12px;
      font-size:16px;font-weight:700;font-family:'Inter',sans-serif;
      cursor:pointer;border:none;text-decoration:none;
      display:inline-flex;align-items:center;gap:8px;
      transition:all 0.2s;
    }
    .btn-hero-primary{
      background:linear-gradient(135deg,var(--blue),var(--blue-mid));
      color:white;
      box-shadow:0 8px 24px rgba(37,99,235,0.35);
    }
    .btn-hero-primary:hover{transform:translateY(-2px);box-shadow:0 12px 32px rgba(37,99,235,0.45)}
    .btn-hero-primary:active{transform:translateY(0)}

    .hero-stats{
      display:flex;align-items:center;justify-content:center;gap:32px;
      margin-top:56px;flex-wrap:wrap;
      animation:fadeUp 0.6s 0.4s ease both;
    }
    .hero-stat{text-align:center}
    .hero-stat-num{font-size:28px;font-weight:800;color:var(--gray-900);letter-spacing:-1px}
    .hero-stat-label{font-size:13px;color:var(--gray-400);margin-top:2px}
    .hero-stat-divider{width:1px;height:36px;background:var(--gray-200)}

    /* ── FEATURES ── */
    .section{padding:96px 24px;max-width:1200px;margin:0 auto}
    .section-label{font-size:12px;font-weight:700;color:var(--blue);letter-spacing:1.5px;text-transform:uppercase;margin-bottom:12px;text-align:center}
    .section-title{font-size:clamp(28px,4vw,42px);font-weight:800;color:var(--gray-900);letter-spacing:-1px;text-align:center;margin-bottom:14px;line-height:1.15}
    .section-sub{font-size:16px;color:var(--gray-500);text-align:center;max-width:520px;margin:0 auto 56px;line-height:1.6}

    .features-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
    .feature-card{
      background:var(--white);border:1px solid var(--gray-200);
      border-radius:16px;padding:28px;
      transition:all 0.2s;
      position:relative;overflow:hidden;
    }
    .feature-card::before{
      content:'';position:absolute;inset:0;
      background:linear-gradient(135deg,var(--blue-light),transparent);
      opacity:0;transition:opacity 0.2s;
    }
    .feature-card:hover{transform:translateY(-4px);box-shadow:0 12px 32px rgba(37,99,235,0.1);border-color:var(--blue-pale)}
    .feature-card:hover::before{opacity:1}
    .feature-icon{
      width:48px;height:48px;border-radius:12px;
      background:var(--blue-light);
      display:flex;align-items:center;justify-content:center;
      margin-bottom:18px;position:relative;z-index:1;
    }
    .feature-title{font-size:17px;font-weight:700;color:var(--gray-900);margin-bottom:8px;position:relative;z-index:1}
    .feature-desc{font-size:14px;color:var(--gray-500);line-height:1.6;position:relative;z-index:1}

    /* ── HOW IT WORKS ── */
    .how-section{background:var(--gray-50);padding:96px 24px}
    .how-inner{max-width:1000px;margin:0 auto}
    .steps-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:24px;margin-top:56px;position:relative}
    .steps-grid::before{
      content:'';position:absolute;top:28px;left:calc(12.5% + 24px);right:calc(12.5% + 24px);
      height:2px;background:linear-gradient(90deg,var(--blue-pale),var(--blue),var(--blue-pale));
      z-index:0;
    }
    .step-item{text-align:center;position:relative;z-index:1}
    .step-num{
      width:56px;height:56px;border-radius:50%;
      background:linear-gradient(135deg,var(--blue),var(--blue-mid));
      color:white;font-size:18px;font-weight:800;
      display:flex;align-items:center;justify-content:center;
      margin:0 auto 16px;
      box-shadow:0 4px 14px rgba(37,99,235,0.3);
    }
    .step-title{font-size:15px;font-weight:700;color:var(--gray-800);margin-bottom:6px}
    .step-desc{font-size:13px;color:var(--gray-500);line-height:1.5}

    /* ── SOCIAL PROOF ── */
    .proof-section{padding:96px 24px;max-width:1100px;margin:0 auto}
    .proof-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
    .proof-card{
      background:var(--white);border:1px solid var(--gray-200);
      border-radius:16px;padding:28px;
    }
    .proof-stars{display:flex;gap:3px;margin-bottom:14px}
    .proof-star{color:#f59e0b;font-size:16px}
    .proof-text{font-size:14px;color:var(--gray-600);line-height:1.65;margin-bottom:18px}
    .proof-author{display:flex;align-items:center;gap:10px}
    .proof-avatar{width:36px;height:36px;border-radius:50%;background:linear-gradient(135deg,var(--blue),var(--blue-mid));display:flex;align-items:center;justify-content:center;color:white;font-weight:700;font-size:14px;flex-shrink:0}
    .proof-name{font-size:14px;font-weight:600;color:var(--gray-800)}
    .proof-role{font-size:12px;color:var(--gray-400)}

    /* ── CTA BANNER ── */
    .cta-section{
      margin:0 24px 96px;
      border-radius:24px;
      background:linear-gradient(135deg,var(--blue-dark) 0%,var(--blue) 50%,var(--blue-mid) 100%);
      padding:72px 48px;
      text-align:center;
      position:relative;overflow:hidden;
    }
    .cta-section::before{content:'';position:absolute;width:400px;height:400px;border-radius:50%;background:rgba(255,255,255,0.05);top:-150px;right:-100px}
    .cta-section::after{content:'';position:absolute;width:300px;height:300px;border-radius:50%;background:rgba(255,255,255,0.05);bottom:-100px;left:-80px}
    .cta-title{font-size:clamp(28px,4vw,44px);font-weight:800;color:white;letter-spacing:-1px;margin-bottom:14px;position:relative;z-index:1}
    .cta-sub{font-size:16px;color:rgba(255,255,255,0.75);margin-bottom:36px;position:relative;z-index:1}
    .btn-cta{
      display:inline-flex;align-items:center;gap:8px;
      padding:16px 36px;border-radius:12px;
      font-size:16px;font-weight:700;font-family:'Inter',sans-serif;
      background:white;color:var(--blue);border:none;cursor:pointer;
      text-decoration:none;
      box-shadow:0 8px 24px rgba(0,0,0,0.15);
      transition:all 0.2s;position:relative;z-index:1;
    }
    .btn-cta:hover{transform:translateY(-2px);box-shadow:0 12px 32px rgba(0,0,0,0.2)}

    /* ── FOOTER ── */
    .footer{border-top:1px solid var(--gray-200);padding:32px 40px;display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:12px}
    .footer-brand{display:flex;align-items:center;gap:8px;font-size:15px;font-weight:700;color:var(--gray-700)}
    .footer-brand-icon{width:28px;height:28px;background:linear-gradient(135deg,var(--blue),var(--blue-mid));border-radius:7px;display:flex;align-items:center;justify-content:center}
    .footer-copy{font-size:13px;color:var(--gray-400)}
    .footer-links{display:flex;gap:20px}
    .footer-link{font-size:13px;color:var(--gray-400);text-decoration:none;transition:color 0.15s}
    .footer-link:hover{color:var(--blue)}

    @keyframes fadeUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}

    /* ── RESPONSIVE ── */
    @media(max-width:900px){
      .features-grid{grid-template-columns:repeat(2,1fr)}
      .steps-grid{grid-template-columns:repeat(2,1fr)}
      .steps-grid::before{display:none}
      .proof-grid{grid-template-columns:1fr}
    }
    @media(max-width:600px){
      .nav{padding:0 20px}
      .nav-links{display:none}  /* hide entire desktop nav on mobile */
      .hamburger{display:flex}
      .features-grid{grid-template-columns:1fr}
      .hero-stats{gap:20px}
      .hero-stat-divider{display:none}
      .cta-section{padding:48px 24px;margin:0 16px 64px}
      .footer{flex-direction:column;align-items:flex-start;padding:24px 20px}
    }
  </style>
</head>
<body>

<!-- NAVBAR -->
<nav class="nav" id="navbar">
  <a href="/" class="nav-brand">
    <div class="nav-brand-icon">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </div>
    PrepPilot
  </a>
  <div class="nav-spacer"></div>
  <div class="nav-links">
    <a href="#features" class="nav-link">Features</a>
    <a href="#how" class="nav-link">How it works</a>
    <a href="/login" class="nav-link">Sign in</a>
    <a href="/register" class="nav-cta">Get started free</a>
  </div>
  <button class="hamburger" id="hamburgerBtn">
    <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="19" y2="6"/><line x1="3" y1="11" x2="19" y2="11"/><line x1="3" y1="16" x2="19" y2="16"/></svg>
  </button>
</nav>

<!-- Mobile drawer — shown only on small screens -->
<div class="mobile-menu" id="mobileMenu">
  <a href="#features" class="nav-link" onclick="closeMobileMenu()">Features</a>
  <a href="#how" class="nav-link" onclick="closeMobileMenu()">How it works</a>
  <a href="/login" class="nav-link">Sign in</a>
  <a href="/register" class="nav-cta">Get started free</a>
</div>

<!-- HERO -->
<section class="hero">
  <div class="hero-grid"></div>
  <div class="hero-orb hero-orb-1"></div>
  <div class="hero-orb hero-orb-2"></div>
  <div class="hero-orb hero-orb-3"></div>

  <div class="hero-content">
    <div class="hero-badge">
      <span class="hero-badge-dot"></span>
      AI-Powered Placement Preparation
    </div>

    <h1 class="hero-title">
      Land your dream job<br/>with <span class="accent">AI by your side</span>
    </h1>

    <p class="hero-sub">
      Personalized roadmaps, ATS resume analysis, AI mock interviews, and DSA tracking — everything a student needs to crack placements, in one platform.
    </p>

    <div class="hero-actions">
      <a href="/dashboard" class="btn-hero btn-hero-primary">
        Explore Now
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
      </a>
    </div>

    <div class="hero-stats">
      <div class="hero-stat">
        <div class="hero-stat-num">10K+</div>
        <div class="hero-stat-label">Students preparing</div>
      </div>
      <div class="hero-stat-divider"></div>
      <div class="hero-stat">
        <div class="hero-stat-num">95%</div>
        <div class="hero-stat-label">ATS score improvement</div>
      </div>
      <div class="hero-stat-divider"></div>
      <div class="hero-stat">
        <div class="hero-stat-num">500+</div>
        <div class="hero-stat-label">Companies cracked</div>
      </div>
      <div class="hero-stat-divider"></div>
      <div class="hero-stat">
        <div class="hero-stat-num">Free</div>
        <div class="hero-stat-label">Forever, no card needed</div>
      </div>
    </div>
  </div>
</section>

<!-- FEATURES -->
<section id="features">
  <div class="section">
    <div class="section-label">Everything you need</div>
    <h2 class="section-title">Built for placement success</h2>
    <p class="section-sub">Six powerful tools working together to take you from zero to offer letter.</p>

    <div class="features-grid">

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#2563eb" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 11l19-9-9 19-2-8-8-2z"/></svg>
        </div>
        <div class="feature-title">AI Personalized Roadmap</div>
        <div class="feature-desc">Get a week-by-week learning plan built around your skills, goals, and target company type. No generic advice — just your path.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#2563eb" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
        </div>
        <div class="feature-title">Resume Builder</div>
        <div class="feature-desc">Build a professional resume from scratch or import your existing one. AI rewrites your bullets to maximize impact and ATS score.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#2563eb" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        </div>
        <div class="feature-title">ATS Resume Analyzer</div>
        <div class="feature-desc">Upload your resume and get an instant ATS score, missing keywords, critical issues, and rewrite suggestions powered by Gemini AI.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#2563eb" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" y1="19" x2="12" y2="23"/></svg>
        </div>
        <div class="feature-title">AI Mock Interviews</div>
        <div class="feature-desc">Practice with a real-time AI interviewer. Choose your role, company type, and focus areas. Get instant feedback on every answer.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#2563eb" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
        </div>
        <div class="feature-title">DSA Activity Tracker</div>
        <div class="feature-desc">Sync LeetCode, Codeforces, CodeChef, GitHub and more. See your activity heatmap, streaks, and ratings all in one dashboard.</div>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#2563eb" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
        </div>
        <div class="feature-title">Skill Assessments</div>
        <div class="feature-desc">Test your knowledge on every subtopic in your roadmap. AI generates questions, scores you, and identifies exactly where to improve.</div>
      </div>

    </div>
  </div>
</section>

<!-- HOW IT WORKS -->
<section id="how" class="how-section">
  <div class="how-inner">
    <div class="section-label">Simple process</div>
    <h2 class="section-title">From signup to offer in 4 steps</h2>
    <p class="section-sub">No complicated setup. Start preparing in under 5 minutes.</p>

    <div class="steps-grid">
      <div class="step-item">
        <div class="step-num">1</div>
        <div class="step-title">Create your profile</div>
        <div class="step-desc">Tell us your skills, academic background, and target role. Takes 3 minutes.</div>
      </div>
      <div class="step-item">
        <div class="step-num">2</div>
        <div class="step-title">Get your AI roadmap</div>
        <div class="step-desc">Receive a personalized week-by-week preparation plan built just for you.</div>
      </div>
      <div class="step-item">
        <div class="step-num">3</div>
        <div class="step-title">Build and optimize</div>
        <div class="step-desc">Create your resume, practice interviews, and track your DSA progress daily.</div>
      </div>
      <div class="step-item">
        <div class="step-num">4</div>
        <div class="step-title">Crack placements</div>
        <div class="step-desc">Walk into interviews confident, prepared, and with a resume that gets shortlisted.</div>
      </div>
    </div>
  </div>
</section>

<!-- SOCIAL PROOF -->
<section>
  <div class="proof-section">
    <div class="section-label">Student stories</div>
    <h2 class="section-title">Trusted by students across India</h2>
    <p class="section-sub">Real results from students who used PrepPilot to land their placements.</p>

    <div class="proof-grid">
      <div class="proof-card">
        <div class="proof-stars">
          <span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span>
        </div>
        <div class="proof-text">"The AI roadmap was exactly what I needed. It knew I was weak in System Design and put it front and center. Got placed at a product company in 3 months."</div>
        <div class="proof-author">
          <div class="proof-avatar">R</div>
          <div>
            <div class="proof-name">Rahul Sharma</div>
            <div class="proof-role">SDE at a product startup · B.Tech CSE</div>
          </div>
        </div>
      </div>

      <div class="proof-card">
        <div class="proof-stars">
          <span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span>
        </div>
        <div class="proof-text">"My resume ATS score went from 42 to 87 after using the analyzer. The rewrite suggestions were incredibly specific. Got 4 interview calls the same week."</div>
        <div class="proof-author">
          <div class="proof-avatar">P</div>
          <div>
            <div class="proof-name">Priya Nair</div>
            <div class="proof-role">Frontend Developer · NIT Trichy</div>
          </div>
        </div>
      </div>

      <div class="proof-card">
        <div class="proof-stars">
          <span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span><span class="proof-star">★</span>
        </div>
        <div class="proof-text">"The mock interview feature is insane. I practiced 20+ sessions before my actual interview. The AI asked the exact same questions I got in the real one."</div>
        <div class="proof-author">
          <div class="proof-avatar">A</div>
          <div>
            <div class="proof-name">Arjun Mehta</div>
            <div class="proof-role">Backend Engineer · IIT Bombay</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA BANNER -->
<div class="cta-section">
  <h2 class="cta-title">Your placement journey starts today</h2>
  <p class="cta-sub">Free forever. No credit card. Just results.</p>
  <a href="/register" class="btn-cta">
    Get started free
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
  </a>
</div>

<!-- FOOTER -->
<footer class="footer">
  <div class="footer-brand">
    <div class="footer-brand-icon">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </div>
    PrepPilot
  </div>
  <div class="footer-copy">© 2025 PrepPilot. Built for students, by students.</div>
  <div class="footer-links">
    <a href="/login" class="footer-link">Sign in</a>
    <a href="/register" class="footer-link">Register</a>
  </div>
</footer>

<script>
  // Navbar scroll effect
  window.addEventListener('scroll', () => {
    const nav = document.getElementById('navbar');
    if (window.scrollY > 20) {
      nav.style.background = 'rgba(255,255,255,0.95)';
      nav.style.boxShadow = '0 2px 16px rgba(0,0,0,0.06)';
    } else {
      nav.style.background = 'rgba(255,255,255,0.85)';
      nav.style.boxShadow = 'none';
    }
  });

  // Hamburger toggle
  const hamburgerBtn = document.getElementById('hamburgerBtn');
  const mobileMenu = document.getElementById('mobileMenu');
  hamburgerBtn.addEventListener('click', () => {
    mobileMenu.classList.toggle('open');
  });
  function closeMobileMenu() {
    mobileMenu.classList.remove('open');
  }
  // Close on outside click
  document.addEventListener('click', (e) => {
    if (!hamburgerBtn.contains(e.target) && !mobileMenu.contains(e.target)) {
      mobileMenu.classList.remove('open');
    }
  });

  // Smooth scroll for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(a => {
    a.addEventListener('click', e => {
      e.preventDefault();
      const target = document.querySelector(a.getAttribute('href'));
      if (target) target.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  });

  // Intersection observer for fade-in animations
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.animation = 'fadeUp 0.5s ease both';
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.feature-card, .step-item, .proof-card').forEach(el => {
    el.style.opacity = '0';
    observer.observe(el);
  });
</script>
</body>
</html>
